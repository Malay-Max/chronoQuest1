version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8550:8000"
    volumes:
      - ./backend/chrono.db:/app/chrono.db
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}

  frontend:
    build: ./frontend
    ports:
      - "3550:80"
    depends_on:
      - backend
